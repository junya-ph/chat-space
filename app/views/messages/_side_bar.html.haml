.side-bar
  .user-name
    .top-item
      .top-item__name
        = current_user.name
      .top-item__icons
        .top-item__icons__edit
          = link_to new_group_path do
            = icon('far', 'edit', class: 'top-item__icons__edit__btn')
        .top-item__icons__cog
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: 'top-item__icons__edit__btn')
  .groups
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group__name
            = group.name
          .group__message
            まだメッセージがありません